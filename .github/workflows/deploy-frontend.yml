name: Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: harbored-hope/frontend/package-lock.json

      - name: Install dependencies
        working-directory: harbored-hope/frontend
        run: npm ci

      - name: Build
        working-directory: harbored-hope/frontend
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
        run: npm run build

      - name: Deploy to Azure Static Web Apps
        uses: azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          # app_location points directly at the Vite build output so Azure serves
          # the production index.html (not the dev one in frontend/). The
          # staticwebapp.config.json is in frontend/public/ and Vite copies it
          # into dist/ automatically — do not move it.
          app_location: 'harbored-hope/frontend/dist'
          output_location: ''
          skip_app_build: true
